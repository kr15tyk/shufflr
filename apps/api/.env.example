# Copy this file to .env and fill in the values

# PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/shufflr?schema=public"

# bcrypt hash of the seed admin password (required to run prisma/seed.ts)
# Generate with: node -e "require('bcryptjs').hash('yourpassword',10).then(console.log)"
SEED_ADMIN_PASSWORD_HASH=""
